<article>
	<ul>
		<% @peeps.each do |peep| %>
			<li class="peep">
				<h1><%= peep.content %></h1>
				<p>Posted by <%= peep.user.username %> (<%= peep.user.name %>)</p>
				<p>Submitted on <%= peep.timestamp %></p>
			</li>
		<% end %>
	</ul>
</article>